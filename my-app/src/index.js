import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import reportWebVitals from './reportWebVitals';
import Käyttäjälista from './Components/Käyttäjälista';

ReactDOM.render(
  <React.StrictMode>
    <Käyttäjälista />
  </React.StrictMode>,
  document.getElementById('root')
);
reportWebVitals();
